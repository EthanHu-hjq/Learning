# 元组的特性跟列表一样，有序、可迭代、可存放不同类型元素、可重复，但是不能够修改元素，元组常用的方法只有index和count两个方法
# 常用方法：
# --index("元素”) : 找到对应元素的索引，如果有多个一样的的元素只会返回第一个的索引
# --count("元素") : 计算该元素在元组中的个数

# 元组的声明：元组名 = (元素1,元素2,.....)
## 声明多个元素的元组
# tuple1 = (1,2,3,'a','b','c')
# tuple2 = 1,2,3,'a','b','c'
## 声明只有一个元素的元组
# tuple3 = 1,




