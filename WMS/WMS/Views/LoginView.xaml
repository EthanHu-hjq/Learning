<UserControl x:Class="WMS.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:WMS.ViewModels"
             d:DataContext="{d:DesignInstance vm:LoginViewModel}"
             xmlns:helpers="clr-namespace:WMS.Helpers"
             xmlns:local="clr-namespace:WMS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- 按钮样式 -->
        <Style TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF4078C0"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- 输入框样式 -->
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#FF909090"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="BorderBrush" Value="#FF909090"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="30" Width="380" Height="240">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 用户名行 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="用户名：" 
               VerticalAlignment="Center" Width="60" FontSize="14"/>
        <TextBox Grid.Row="0" Grid.Column="1" 
             Text="{Binding Member.Name}" 
             Height="32" FontSize="14" Margin="10,4,0,12"/>

        <!-- 密码行（推荐PasswordBox，安全输入密码，原生WPF没有Password的直接绑定，后面给附加属性方案） -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="密　码：" 
               VerticalAlignment="Center" Width="60" FontSize="14"/>
        <PasswordBox Grid.Row="1" Grid.Column="1"
                 helpers:PasswordBoxHelper.Password="{Binding Member.Password,UpdateSourceTrigger=PropertyChanged}" 
                 Height="32" FontSize="14" Margin="10,4,0,12"/>

        <!-- 按钮区域，水平居中 -->
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="登录" 
                Command="{Binding LoginCommand}" 
                CommandParameter="Login"
                Width="100" Height="34" FontSize="14" Margin="0,0,15,0"/>
            <Button Content="退出" 
                Command="{Binding LoginCommand}" 
                CommandParameter="Exit"
                Width="100" Height="34" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>